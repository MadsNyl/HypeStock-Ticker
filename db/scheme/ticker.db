CREATE TABLE ticker (
    symbol varchar(10),
    name varchar(255),
    exchange varchar(255),
    legacy boolean DEFAULT FALSE,
    created_date datetime DEFAULT NOW(),
    cik int,
    PRIMARY KEY (symbol)
);